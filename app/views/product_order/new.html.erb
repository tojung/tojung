<%= form_tag '/product_order' do %>
  <button class="btn btn-light order-btn mb-3">주문하기</button>
    <% if @package.price != 0 %>
    <%= hidden_field_tag 'delivery_price', '3000' %>
    <%= hidden_field_tag 'total_price', @package.price + 3000 %>
  <% else %>
    <%= hidden_field_tag 'delivery_price', '0' %>
    <%= hidden_field_tag 'total_price', @package.price %>
  <% end %>

    <%= hidden_field_tag 'payment_method', '무통장입금' %>
    <%= hidden_field_tag 'product_id', @product.id %>
    <%= hidden_field_tag 'package_id', @package.id %>
    <%= hidden_field_tag 'user_id', current_user.id %>
  <div class="package-selected-wrapper bg-white mb-3 mx-0 px-3 py-3">
    <div class="package-selected d-flex">
      <%=image_tag(@product.image0, class: "order-img")%>
      <div class="package-info pl-3">
        <h4 class="font-14 font-weight-bold"><%= @product.name %></h4>
        <p class="font-grey font-14 mb-0"><%= @package.name %></p>
        <p class="font-grey font-14 package-info"><%= @package.content %></p>
        <!-- css 적용 필요 -->
      </div>
    </div>
    <p class="font-14 font-weight-bold order-money text-right mb-2"><%= number_field_tag 'product_price', @package.price, min: @package.price, onchange: 'change_product_price();', onkeydown: "if (event.keyCode == 13) { return false;}"%>
      <span class="order-money-won">원</span></p>
    <p class="font-12 text-right mb-0">더 많은 후원을 하고 싶다면 금액을 수정해주세요. </p>
  </div>

  <!-- 주문 정보 -->
<div class="order-user-info font-14 mb-3 py-3 px-3 bg-white">
  <h4 class="font-18 font-weight-bold pb-2 border-bottom-black">주문 고객 정보</h4>
    <div class="form-group row py-2 mb-0 mx-0">
      <label for="user_name" class="col-4 px-0 col-form-label">주문 고객명</label>
      <div class="col-8 px-0">
<!--        <input type="text" class="form-control px-0" id="user_name" placeholder="이름을 입력해 주세요.">-->
        <%= text_field_tag 'name', current_user.name, placeholder: '이름을 입력해 주세요.', class: 'form-control px-0', onkeydown: "if (event.keyCode == 13) { return false;}" %>
      </div>
    </div>
    <div class="form-group row py-2 mb-0 mx-0">
      <label for="phone_num" class="col-4 px-0 col-form-label">휴대폰 번호</label>
      <div class="col-8 px-0">
<!--        <input type="tel" class="form-control px-0" id="phone_num" placeholder="휴대폰 번호를 (-) 빼고 입력해주세요">-->
        <input class="form-control px-0" type="tel" name="phone_num" value="<%= current_user.phone_number %>" placeholder="휴대폰 번호를 (-) 포함해서 입력해주세요" pattern="[0-9]{3}-[0-9]{4}-[0-9]{4}" onkeydown="if (event.keyCode == 13) { return false;}" required>
      </div>
    </div>
    <div class="form-group row py-2 mb-0 mx-0">
      <label for="user_email" class="col-4 px-0 col-form-label">이메일 주소</label>
      <div class="col-8 px-0">
<!--        <input type="email" class="form-control px-0" id="user_email" placeholder="이메일 주소를 입력해 주세요.">-->
        <input type="email" name="email" placeholder="이메일 주소를 입력해 주세요." value="<%= current_user.email %>" class="form-control px-0" onkeydown="if (event.keyCode == 13) { return false;}" required>
      </div>
    </div>
    <div class="form-group row py-2 mb-0 mx-0">
      <label for="sample2_postcode" class="col-4 px-0 col-form-label">우편번호</label>
      <div class="col-8 px-0">
        <input class="form-control px-0" type="text" id="sample2_postcode" value="<%= current_user.address_num %>" placeholder="우편번호" name="address_num" onkeydown="if (event.keyCode == 13) { return false;}" required>
        <input type="button" class="bg-white btn-post" onclick="sample2_execDaumPostcode()" value="우편번호 찾기"><br>
      </div>
      <!-- iOS에서는 position:fixed 버그가 있음, 적용하는 사이트에 맞게 position:absolute 등을 이용하여 top,left값 조정 필요 -->
      <div id="layer" style="display:none;position:fixed;overflow:hidden;z-index:1;-webkit-overflow-scrolling:touch;">
        <img src="//t1.daumcdn.net/localimg/localimages/07/postcode/320/close.png" id="btnCloseLayer" style="cursor:pointer;position:absolute;right:-3px;top:-3px;z-index:1" onclick="closeDaumPostcode()" alt="닫기 버튼">
      </div>
    </div>

    <div class="form-group row py-2 mb-0 mx-0">
      <label for="sample2_address" class="col-4 px-0 col-form-label">주소</label>
      <div class="col-8 px-0">
        <input type="text" id="sample2_address" value="<%= current_user.address_text %>" class="form-control px-0" placeholder="주소" name="address_text" onkeydown="if (event.keyCode == 13) { return false;}" required>
      </div>
    </div>
    <div class="form-group row py-2 mb-0 mx-0">
      <label for="extra_address" class="col-4 px-0 col-form-label">상세주소</label>
      <div class="col-8 px-0">
        <input type="text" class="form-control px-0" name="address_text2" value="<%= current_user.address_extra %>" placeholder="상세 주소" onkeydown="if (event.keyCode == 13) { return false;}" required>
      </div>
    </div>
  </div>
  <!-- //주문정보 -->

  <!-- 국회 청원  -->
  <div class="bg-white mb-3 px-3 py-3">
    <h4 class="font-18 font-weight-bold pb-2 border-bottom-black">국회 청원 내용</h4>
    <pre class="text-center font-14 my-3">
  내 인생으로 배송하기를 누르면
  이 법안의 심사를 담당하고 있는 ‘<%= @product.assos %>’
  국회의원 <%= @product.maker_responses.count %>명에게
  청원 이메일이 자동으로 전달됩니다.</pre>
<!--      <textarea name="" id="" cols="30" rows="10" placeholder="안녕하세요, 000 의원님!-->
<!--여성가족 위원회에 계류되어있는 2006073 의안인 ‘데이트폭력법’이 왜 통과되지 않는지 궁금합니다.-->
<!--000 의원님은 이 입법안에 대해 어떤 의견을 가지고 계시나요? 이 입법을 꼭 빠르게 추진해주세요.-->
<!--이메일 청원합니다."></textarea>-->
      <%= text_area_tag 'send_email_content', "안녕하세요, 의원님!
#{@product.assos}에 계류되어있는 #{@product.bill_id} 의안인 '#{@product.bill_name}'이 왜 통과되지 않는지 궁금합니다.
의원님은 이 입법안에 대해 어떤 의견을 가지고 계시나요? 이 입법을 꼭 빠르게 추진해주세요.
이메일 청원합니다.", class: "form-control px-3 py-3 font-14", cols: 30, rows: 10, onkeydown: "if (event.keyCode == 13) { return false;}"%>
  </div>
  <!-- // 국회 청원  -->

  <!-- 주문 금액 -->
  <div class="bg-white mb-3 font-14 py-3">
    <h4 class="font-18 font-weight-bold pb-2 border-bottom-black mx-3">주문 금액</h4>
    <div class="d-flex justify-content-around order-card-row">
      <div class="order-card">
        <p class="mb-0">총 상품 금액</p>
        <p class="font-weight-bold mb-0" id="product_price_description"><%= @package.price%>원</p>
      </div>
      <div class="order-card">
        <p class="mb-0">국회 청원</p>
        <p class="font-weight-bold mb-0">이메일 <%= @product.maker_responses.count %>건</p>
      </div>
      <div class="order-card">
        <% if @package.price != 0%>
        <p class="mb-0">배송비</p>
        <p class="font-weight-bold mb-0">3,000원</p>
          <% else %>
          <p class="mb-0">배송비</p>
          <p class="font-weight-bold mb-0">0원</p>
        <% end %>
      </div>
    </div>
    <% if @package.price != 0 %>
    <p class="font-18 text-right mr-3">최종 결제금액 <span class="font-weight-bold" id="product_price_description2"><%= @package.price + 3000 %>원</span></p>
      <% else %>
      <p class="font-18 text-right mr-3">최종 결제금액 <span class="font-weight-bold" id="product_price_description2"><%= @package.price %>원</span></p>
  <% end%>
      <label class="checklabel ml-2">광고 후원 굿즈는 <%= (@product.goods_dilivery_date.month)%>/<%=@product.goods_dilivery_date.day %>일에 일괄 배송 시작됩니다.
      <input type="checkbox" class="checkbox"><span class="checkmark"></span>
    </label>
    <label class="checklabel ml-2">개인정보 수집 및 이용에 동의합니다.
      <input type="checkbox" class="checkbox"><span class="checkmark"></span>
    </label>
  </div>
  <!-- // 주문 금액 -->

  <!-- 결제 -->
  <div class="bg-white font-14 font-weight-bold px-3 py-3">
    <h4 class="font-18 pb-2 font-weight-bold border-bottom-black mb-3">결제수단</h4>
    <div class="d-flex flex-wrap justify-content-around">
      <button type="button" class="pay-button mb-3" onclick="normal_payment();">무통장 입금</button>
      <button type="button" class="pay-button mb-3" onclick="card_payment();">신용카드</button>
      <button type="button" class="pay-button mb-3" onclick="extra_payment();">휴대폰</button>
      <button type="button" class="pay-button mb-3" onclick="extra_payment();">TOSS</button>
    </div>
    <div id="payment_method_description">
    </div>
<!--    <button class="btn-pay-finish text-center my-4">내 인생으로 배송받기</button>-->
    <%= submit_tag '내인생으로 배송받기', class: 'btn-pay-finish text-center my-4' %>
  </div>
<% end %>

<script>
    // 우편번호 찾기 화면을 넣을 element
    var element_layer = document.getElementById('layer');

    function closeDaumPostcode() {
        // iframe을 넣은 element를 안보이게 한다.
        element_layer.style.display = 'none';
    }

    function sample2_execDaumPostcode() {
        new daum.Postcode({
            oncomplete: function(data) {
                // 검색결과 항목을 클릭했을때 실행할 코드를 작성하는 부분.

                // 각 주소의 노출 규칙에 따라 주소를 조합한다.
                // 내려오는 변수가 값이 없는 경우엔 공백('')값을 가지므로, 이를 참고하여 분기 한다.
                var fullAddr = data.address; // 최종 주소 변수
                var extraAddr = ''; // 조합형 주소 변수

                // 기본 주소가 도로명 타입일때 조합한다.
                if(data.addressType === 'R'){
                    //법정동명이 있을 경우 추가한다.
                    if(data.bname !== ''){
                        extraAddr += data.bname;
                    }
                    // 건물명이 있을 경우 추가한다.
                    if(data.buildingName !== ''){
                        extraAddr += (extraAddr !== '' ? ', ' + data.buildingName : data.buildingName);
                    }
                    // 조합형주소의 유무에 따라 양쪽에 괄호를 추가하여 최종 주소를 만든다.
                    fullAddr += (extraAddr !== '' ? ' ('+ extraAddr +')' : '');
                }

                // 우편번호와 주소 정보를 해당 필드에 넣는다.
                document.getElementById('sample2_postcode').value = data.zonecode; //5자리 새우편번호 사용
                document.getElementById('sample2_address').value = fullAddr;

                // iframe을 넣은 element를 안보이게 한다.
                // (autoClose:false 기능을 이용한다면, 아래 코드를 제거해야 화면에서 사라지지 않는다.)
                element_layer.style.display = 'none';
            },
            width : '100%',
            height : '100%',
            maxSuggestItems : 5
        }).embed(element_layer);

        // iframe을 넣은 element를 보이게 한다.
        element_layer.style.display = 'block';

        // iframe을 넣은 element의 위치를 화면의 가운데로 이동시킨다.
        initLayerPosition();
    }

    // 브라우저의 크기 변경에 따라 레이어를 가운데로 이동시키고자 하실때에는
    // resize이벤트나, orientationchange이벤트를 이용하여 값이 변경될때마다 아래 함수를 실행 시켜 주시거나,
    // 직접 element_layer의 top,left값을 수정해 주시면 됩니다.
    function card_payment() {
        var payment_method =  document.getElementById('payment_method_description');
        payment_method.innerHTML = "무통장입금을 선택하였습니다.<br><button type='button' onclick='pay_new_test()'>결제창 버튼</button>";
    }
    function initLayerPosition(){
        var width = 300; //우편번호서비스가 들어갈 element의 width
        var height = 400; //우편번호서비스가 들어갈 element의 height
        var borderWidth = 5; //샘플에서 사용하는 border의 두께

        // 위에서 선언한 값들을 실제 element에 넣는다.
        element_layer.style.width = width + 'px';
        element_layer.style.height = height + 'px';
        element_layer.style.border = borderWidth + 'px solid';
        // 실행되는 순간의 화면 너비와 높이 값을 가져와서 중앙에 뜰 수 있도록 위치를 계산한다.
        element_layer.style.left = (((window.innerWidth || document.documentElement.clientWidth) - width)/2 - borderWidth) + 'px';
        element_layer.style.top = (((window.innerHeight || document.documentElement.clientHeight) - height)/2 - borderWidth) + 'px';
    }
    function normal_payment(){
        var payment_method =  document.getElementById('payment_method_description');
        payment_method.innerText = '무통장입금을 선택하였습니다.';
    }
    function extra_payment(){
        var payment_method =  document.getElementById('payment_method_description');
        payment_method.innerText = '해당 옵션은 현재 제공하지않습니다.';
    }
    function change_product_price() {
        var product_price = parseInt(document.getElementsByName('product_price')[0].value);
        var product_price_description = document.getElementById('product_price_description');
        var product_price_description2 = document.getElementById('product_price_description2');
        var total_price = document.getElementById('total_price');
        product_price_description.innerText = product_price;
        <% if @package.price != 0 %>
          product_price_description2.innerText = product_price + 3000;
          total_price.value = product_price + 3000;
        <% else %>
        product_price_description2.innerText = product_price;
        total_price.value = product_price;
        <% end %>
        console.log("changed");
    }
    function pay_new_test() {
        $(document).ready(function () {
            var IMP = window.IMP;
            IMP.init('imp03714372');
            IMP.request_pay({
                pg: 'danal',
                pay_method: 'card',
                merchant_uid: 'merchant_' + new Date().getTime(),
                name: '주문명_' + new Date().getTime(),
                amount: $('input[name=total_price]').val(),
                buyer_email: $('input[name=email]').val(),
                buyer_name: $('input[name=user_name]').val(),
                buyer_tel: $('input[name=phone_num]').val(),
                buyer_addr: $('input[name=address_text]').val(),
                buyer_postcode: $('input[name=address_num]').val(),
                digital: 'true',
            }, function (rsp) {
                if (rsp.success) {
                    var msg = '결제가 완료되었습니다.';
                    msg += '고유ID : ' + rsp.imp_uid;
                    msg += '상점 거래ID : ' + rsp.merchant_uid;
                    msg += '결제 금액 : ' + rsp.paid_amount;
                    msg += '카드 승인번호 : ' + rsp.apply_num;
                } else {
                    var msg = '결제에 실패하였습니다.';
                    msg += '에러내용 : ' + rsp.error_msg;
                }
            })
        });
    }
</script>
